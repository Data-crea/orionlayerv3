{
  "_note": "THE COLONY MANAGEMENT SCREEN'S GEOMETRY, AND THE ONLY PLACE IT IS TYPED. Every rectangle is in the 1920x1080 reference space (core/layout.py). tools/frame_build.py builds the frame from this file plus the master's ring and texture; tools/frame_mask.py renders the windows; tools/frame_holes.py derives the cutout boxes back out of the built frame, and a smoke check asserts holes == mask == boxes.json at every resolution (decision 3). So a rectangle changes HERE and nowhere else, and everything downstream is generated - decision 1 and decision 40.",

  "ring": {"left": 107, "right": 120, "top": 18, "bottom": 74},
  "_ring_source": {
    "file": "screens/galaxy_map/assets/frame.png",
    "tool": "threshold sweep, alpha < 8 / 16 / 64, bbox stable at all three",
    "date": "2026-09-06",
    "note": "THE FAMILY RING, FOUR VALUES, AND THE ASYMMETRY IS THE ARTWORK'S AND NOT AN ERROR. The master's metal is wide at the sides, thin at the top and medium at the bottom; a single number cannot describe it, which is why `bezel` is gone. Measured on the main-screen master because that is what this screen has to sit beside, and mapped to the reference space the same way the running screen maps it - the frame image is stretched over the whole reference area (screen._scale_frame), so each axis scales independently. A HAND-COPIED NUMBER GETS A CHECKER (decision 36): the smoke test re-measures that file with the same sweep and fails if the table and the artwork ever disagree. The shipped colony frame is NOT yet this ring - it measures 100/103/12/70 - and tools/frame_build.py is what makes it so."
  },

  "header": [107, 18, 1693, 48],
  "list": [107, 74, 1693, 652],

  "list_columns": {
    "name": 302,
    "farmers": 347,
    "workers": 347,
    "scientists": 347,
    "building": 314,
    "scroll": 36
  },
  "_list_columns_note": "302 + 347*3 + 314 + 36 = 1693, which is the list's own width exactly - the columns ARE the list, with no slack to distribute. THE LAYOUT YIELDED TO THE RING on 7 September 2026: at a 44 px margin the list was 1832 wide with 394 px figure columns, and the family ring takes 139 of that. The two pixels that do not divide by three went to the name column (300 -> 302), the only one with no derivation to break. CAPACITY IS ASSERTED, NOT NOTED: a figure column must fit at least four unsqueezed figures, which is what the original's widest column (industry, reach 122, pitch 30) fits. At 347 reference px it fits five, and five at every resolution, because the column and the pitch scale together. THE BUILDING COLUMN IS DERIVED: the original prints the producing text with BILL::Squeeze_Print_Formatted_Paragraph_(0x200, y, 0x55, 0x16, ...) at colsum.cpp:621 - 85 px of a 640 px screen, 13.3 % - and 13.3 % of 1693 is 225. 314 is wider than the transcription requires; the surplus is the extra HD width that Data's Stage 1 decision sends to the column reservation rather than to wider figure spacing. See colonybuild.py for why the width is a RESERVATION and never a squeeze target.",

  "planet_info": [107, 742, 320, 220],
  "planet_output": [445, 742, 746, 220],
  "galaxy_inset": [1209, 752, 253, 200],
  "empire_stats": [1480, 742, 320, 220],
  "_lower_band_note": "Four boxes, flush with the list's own edges at 107 and 1800, with EQUAL gaps of 18. planet_output absorbs whatever the arithmetic leaves, because it is the only width in the band that is not fixed by something else - the two 320s are a pair and 253 is locked by the inset's aspect (see _galaxy_inset_note). 107 + 320 + 18 + 746 + 18 + 253 + 18 + 320 = 1800. The inset is 20 px shorter than its neighbours and is centred against them (742 + (220-200)/2 = 752), because its height is fixed by its own aspect and not by the band.",
  "_galaxy_inset_note": "253 x 200 IS DERIVED AND CONFIRMED - doc/colony_inset_geometry.md Part 3. The original's inset covers 50.6*M x 40*M world units, whose aspect is 1.2651 at EVERY galaxy size because both divisors are constants and M cancels (movebox.cpp:20-21). 253/200 = 1.2650 against that, so the crop fills the box with no letterbox at any size INCLUDING orion2re's Maximum - the earlier expectation that Maximum letterboxes is WITHDRAWN, and it only did so if the crop were the galaxy rather than the original's coverage. The scale that follows is exactly 5/M reference px per world unit. At 3840x2160 the box is 506 x 400 device px, which is the small galaxy's own world extent at 1:1.",

  "sort_bar": [107, 974, 1393, 32],
  "return_button": [1512, 974, 288, 32],

  "figure_scale": {"1920x1080": 2, "2560x1440": 3, "3840x2160": 4},
  "_figure_scale_note": "INTEGER STEPS, and that is the whole reason the table exists. A population figure is 28 x 28 native px (RACEICON.LBX, every people entry), and the layout scales are 1, 4/3 and 2 - so proportional scaling would want 2, 2.667 and 4, and 2.667 is not an integer. Nearest-neighbour at a fractional step invents pixels the original does not have, which a reference figure may not do. 2 / 3 / 4 gives 56 / 84 / 112 px. THIS FILE is the only place that changes. Marked HD EXTENSION in core/zoomtables.py.",

  "_resolutions": ["1920x1080", "2560x1440", "3840x2160"]
}
