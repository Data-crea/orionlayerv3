# Work order 172 — parked for Data

Every choice this unattended run made that is Data's to confirm, with
the default taken.

---

## P1 — How the plate stays clean

**Default:** the whole plate is recoloured (component rule, lamps kept),
scaled by one smooth curve with no edge floors, so its painted glow
stays a gradient. **Alternatives, not built:** a code-drawn glow, or
fading the glow with saturation. Renders: `title_plate_tones_after.png`.

## P2 — The checkbox tick

**Default:** a tick in the lit edge colour on the small button's
square, following the frame colour; unchecked is the empty square.
New Game's toggles and the GAME menu's settings use it. The old knob
pictures (`toggle_on/off.png`) stay in the assets, unused.

## P3 — The live part

Not run: Data's engine holds the port and may not be connected to.
