Repo klonen, doc/v3_fundament.md lesen.
Outpost-Filter: col.outpost_flag != 0 als zweite Bedingung, Quelle colxport.cpp:91-99. Erst committen, wenn Offset 6 seine zweite Quelle hat — die Zählprobe im Referenz-Save. Die Planet-Prüfung getrennt kommentieren, sie ist nicht Teil der Transkription.
Tie-Break raus: _by() gibt nur -r[field] zurück. Kommentarblock neu mit ext_api.cpp:94, colxport.cpp:91, colsum.cpp:363 und colsum.cpp:1056. Smoke-Check umdrehen auf „Gleichstände behalten die Eingabereihenfolge", geprüft über build_rows.
Sortierleiste auf Hotkey umstellen: _inject bekommt einen zweiten Weg über inject_key mit spec["hotkey"], die native_click-Punkte bleiben als Rückfall stehen und behalten ihre Quelle. Vorher am laufenden Spiel prüfen, ob die Taste ankommt und ob der Cursor springt — erst danach umschalten, denn ein stiller Fehlschlag sieht hier genauso aus wie ein Erfolg.
Neu für die Dokumentation, weil es beim Lesen aufgefallen ist und sonst wieder verloren geht: der Zeilenstring des Originals trägt zwei Zustände, die HD nicht zeichnet — Blockade des Sterns (star->blockaded, Bitmaske pro Spieler, colsum.cpp:561) und ein Kolonie-Ereignis (EVENTS::Colony_Has_Event_, colsum.cpp:553) mit eigener Einfärbung und angehängtem Text. Als NICHT GEZEICHNET in colonyrows.py vermerken, nicht als Aufgabe verkleiden.
Doku im selben Commit, Smoke-Test grün, dann Commit. Für die neuen Einträge Datei-Zeilen-Verweise schreiben, keine weiteren C++-Zitate — die Lizenzfrage bei Joe ist offen und die 159 zitierten Zeilen sind schon mehr, als mir lieb ist.