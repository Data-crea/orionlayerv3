First: commit Stage B/C as its own commit, as reported at its Stop 2. Data pushes before any Stage 4 code lands.

Decisions:

Struts: keep A3's master rails. Marked as DEVIATION from the original's proportion, with the numbers (original 15–18 ref px, ours 26–38) and the cost (list −29, planet_output −69, rows unchanged) in the status document. Fix frame_build.py's stale "STRUTS ARE PLAIN" docstring in the same commit; it contradicts what A3 built.

Header: five thin_border plates plus the scroll slot, following the original's structure. Colour deviation lives in colors.json, marked. One drawing question before you commit to it: render two crops beside the native header at the same scale — (a) the plates inside the existing header window, (b) the header hole closed in the plate and the plates on bare metal as the original has them. State for (b) what changes: plate rebuild, mask, frame_holes, and that header becomes the first non-cutout box in this screen's boxes.json. Data picks from the crops.

Job columns: equal, unless the original's 134/141/133 have a source in the code. Say whether colsum.cpp or a table beside it carries those widths. If it does, that is a transcription and we take it; if they are only measurable off the screen, they are artwork pixels and we keep the reservation as transcribed.

Then Stop 2 as briefed: boxes.json swap with the rewritten naming rule in the same commit, all readers re-pointed (screen.py, colony_list_preview.py, smoke_test.py), --write reporting kept 0 and why, three panels, sort row and RETURN, the inset with the dot image at three resolutions. The flag-off semantics stated. Screen beside native at all three resolutions. No push.