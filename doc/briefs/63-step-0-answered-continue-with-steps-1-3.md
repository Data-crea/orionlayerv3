Step 0 answered. Continue with Steps 1–3.

The ring is the family ring, four values, measured from the chosen candidate file, never smoothed. Asymmetry is a property of the artwork, not an error.
bezel is removed. layout_reference.json gets ring: {left, right, top, bottom} with source (file, tool, date) beside it. Smoke test: measure the shipped frame with the same threshold sweep and assert equality with the table; the existing "windows start inside the ring" check runs against those values. Decision 36 pattern — a hand-copied number gets a checker.
The layout yields: list and figure columns shrink to fit inside the ring; _list_columns_note must add up again. Figure capacity per column stays ≥ 4 unsqueezed at every resolution — assert it, don't note it.
Proceed with Steps 1–3 as briefed. For …772970, explain the 0/0 top/bottom: does the file fill the canvas vertically, and are header window and slot band inside the ring or inside the mask area? That decides whether the top and bottom bands are holes in the artwork or boxes in JSON.
background.png stays a derivative in the manifest; nothing is derived from it.

Reporting stop after Step 3, as before.