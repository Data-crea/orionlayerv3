Stage C decided: derived, as recommended. Decision 49 under Sizing and artwork, with asset as the rejected option and the history cost as the reason. Both conditions are part of the decision: frame_build.py in setup.py STEPS, and the rebuild check that reports "absent, run this" rather than skipping.

Header: deferred to Stage 4 Stop 1, where the list columns and the sort row are laid out. Stage B builds with the current full-width window, recorded in the status document as an interim that will change — name it there, so the preview is not mistaken for the intended header. Two things for Stage 4's Stop 1, not now: verify whether the column reservation (302 + 347·3 + 314 + 36) maps onto five plates plus the scroll arrow, and whether the master carries a raised-plate element with a rounded bevel or one has to come from elsewhere.

Go for Stage B and C as laid out at Stop 1, plus:

The two stale claims fixed in this commit: the .gitignore comment says what frames/ is now and where the master lives; setup.py gains the step.
_find_best_fallback made public from its one home, not copied.
The --profiles artefact: either exclude hole pixels from the band average so a chamfered hole measures correctly, or label the reading as bounding-box-based in the tool's output — whichever is cheaper, but the number must not keep claiming "no lit edge" for a hole that has one. Say which you did.
The carried-over STOCK_MAX_MAP_SCALE assertion.
Fundament: decision 49 as above; a section-2 line under Evidence for the profile artefact if it does not already fall under "a measurement not stable under its own threshold" — your call, say which.

Stop 2 as briefed: flag-off hash proof, flag-on log line, smoke count, fundament diff, evidence images under ~/Bilder/rahmen/ with hash in the name. No push.