Go for Stop 2, with these additions.

Probe. Scratch-directory cfg is approved. Write-run-restore is not: a restore is a step that gets forgotten, and a permanently altered star count would surface in a later session as a mystery. If the scratch route fails, first state whether the cfg is a file inside Joes' source tree or a runtime file beside the executable; if it is runtime, a copy of the runtime directory with its own cfg is the next option. Zoom-out via field 9 only, throttled, per decision 35. After the probe, load the reference save once and confirm zoom_check.py reports 36 on it again — the probe must leave no state behind.

Two red runs, not one. The transcription fixes two defects (rounding, and the dropped y term), so the check has to go red on each separately: once with the old estimate reinstated, once with ceilings on x only. Paste both failing outputs.

Fixed points in the test. The independent reference written out in the test; the four stock literals as fixed points; the probed 155-star value pinned with its source line ("live probe 2026-09-07, 155 stars"); and the range figures you reported (differing counts, never-below). If the reported count does not reproduce after implementation, that is a finding.

smoke_test.py:1244. Replace the synthesized 2277 with an extent a 150-unit grid can produce. _FakeGS gets map_max_y. Check whether any other fake game state in the tree lacks it — a fake that omits a field the real snapshot carries is how the y term goes untested.

50.6 in the reach sense. Keep it only where it is re-sourced to movebox.cpp directly, never to the retired constant. One number with two meanings in one tree needs the meaning next to it every time.

A question to verify, not a finding. The fundament, section 3, says the game's native viewport at max scale is 505×399; your ceilings divide by 506 and 400. Are those the same rectangle (inclusive vs. exclusive edge) or two different constants? State which, and make the fundament bullet consistent with the answer.

Fundament. Section 3 bullet rewritten: the expression, its source, five sizes covered, y term included. The three queued paragraphs as briefed. Next free decision number only if something here is a decision — I still expect none.

Stop 2 report as briefed. No push.