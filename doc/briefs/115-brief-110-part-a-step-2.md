108/109 mit Teil B eintragen, nicht separat. Weiter mit Teil A, Schritt 2.

Schritt 2: HD zeichnet Systemfenster und Flottenbox selbst. Vorgaben:

Sternidentität über den letzten HD-Klick plus Konsistenzprüfung; bei Mismatch wird der Kasten nicht gezeichnet (A2).
Schließen über das CLOSE-Feld des Kastens (ESC-Hotkey), kein positionsbasierter Rechtsklick (Entscheidung 66).
Das Kasten-Zeichnen kommt in ein eigenes Modul unter screens/galaxy_map/, nicht in screen.py (liegt schon bei 554 Zeilen).
Falls der Flottenkasten „N turns to …" selbst nachbaut statt es aus dem Framebuffer zu übernehmen: Linie ab dem Befehl, ETA erst ab der nächsten Runde (aus Lauf 114).

Fundament-Einträge zu A4/A5 und die Icon-Größe am Sprite (für Teil B) mit dem jeweiligen Commit, der sie einlöst — nicht vorab.

Nur eine Instanz am Server, wenn es live geht.
